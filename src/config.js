export const defaultTableSize = 100;

export const defaultTitle = 'Новая таблица';

export const defaultStyles = {
  textAlign: 'left',
  fontWeight: 'normal',
  textDecoration: 'none',
  fontStyle: 'normal'
};

export const defaultDelay = 300;

export default {
  defaultTableSize,
  defaultDelay,
  defaultTitle,
  defaultStyles
};
